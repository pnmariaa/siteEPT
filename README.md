# Currículos 3º CPM — transcrição do PDF

Arquivos transcritos:

- `index.html`
- `curriculo.html`
- `vagas.html`
- `certificados.html`
- `styles.css`

Observações:

- As URLs que foram quebradas entre linhas pelo PDF foram reunidas em uma única linha.
- A estrutura, os textos, as classes e os estilos foram mantidos conforme aparecem no PDF.
- O código original apresenta `<button class="chatbot">` sem fechamento explícito; isso foi mantido para preservar a transcrição.
- Não há JavaScript no material enviado.
